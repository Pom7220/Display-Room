- name: Setup Gradle 7.2
  uses: gradle/actions/setup-gradle@v4
  with:
    gradle-version: '7.2'

- name: Build debug APK
  working-directory: boot-launcher
  run: gradle assembleDebug --no-daemon --stacktrace  ← no ./gradlew
